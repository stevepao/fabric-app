# Optional — used only when you start Cloudflare Tunnel alongside the stack:
#   docker compose --profile cloudflare up -d
#
# Create a tunnel in Cloudflare Zero Trust (Networks → Tunnels), copy the token here,
# then route your hostname’s origin to: http://web:80 (Docker service name + Apache port).

TUNNEL_TOKEN=
